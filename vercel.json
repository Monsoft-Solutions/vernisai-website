{
    "rewrites": [
        { "source": "/api/(.*)", "destination": "/api/$1" },
        { "source": "/(.*)", "destination": "/index.html" }
    ],
    "buildCommand": "npm run build && cd packages/database && npm run build",
    "outputDirectory": "apps/website/dist",
    "functions": {
        "api/**": {
            "memory": 1024,
            "maxDuration": 10
        }
    },
    "installCommand": "npm install"
}
