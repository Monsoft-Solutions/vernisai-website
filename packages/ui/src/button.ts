// This is a placeholder file that will be replaced with the actual button component
// when we move components from the website to the UI package

const Button = () => {
  // Placeholder
  return null;
};

export default Button;