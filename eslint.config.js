// @ts-check

import sharedConfig from './packages/config/eslint/eslint.config.js';

// This config is used by the pre-commit hook and for linting files in the root directory
export default sharedConfig;
