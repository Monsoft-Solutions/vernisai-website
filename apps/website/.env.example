# Environment Configuration
# This file serves as a template for the required environment variables.
# Copy this file to .env and fill in the values.
# Variables marked with [REQUIRED] must be set for the application to work.

# Node environment: development, test, or production
NODE_ENV=development

# Vite specific variables (must be prefixed with VITE_)
# These variables will be exposed to the client-side code

# API URL for backend services [REQUIRED]
# VITE_API_URL=http://localhost:3000/api

# Feature flags
VITE_ENABLE_ANALYTICS=false
VITE_ENABLE_FEATURE_X=false

# Analytics and tracking
# Only required if VITE_ENABLE_ANALYTICS is set to true
VITE_GOOGLE_ANALYTICS_ID=
VITE_MICROSOFT_CLARITY_ID=


# Third-party service credentials
# Add any API keys or service credentials needed for the application
# VITE_SERVICE_API_KEY=

# Note: Never commit the actual .env file to version control
# Only environment variables prefixed with VITE_ will be exposed to your client-side code