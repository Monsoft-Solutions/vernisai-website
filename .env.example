# VernisAI Website Environment Configuration
# Copy this file to .env for local development

# Node environment: development, test, or production
NODE_ENV=development

# Vite specific variables (must be prefixed with VITE_)
# These variables will be exposed to the client-side code

# API URL for backend services
VITE_API_URL=http://localhost:3000/api

# Feature flags
VITE_ENABLE_ANALYTICS=false
VITE_ENABLE_FEATURE_X=false

# Analytics and tracking (only needed in production)
# VITE_GOOGLE_ANALYTICS_ID=
# VITE_MICROSOFT_CLARITY_ID=

# Note: 
# 1. Never commit actual .env files to version control
# 2. Only variables prefixed with VITE_ will be exposed to client-side code
# 3. For development you can place .env files either:
#    - In the project root directory
#    - In the apps/website directory (these will override root values)
# 4. For Vercel deployment, set these in the Vercel project settings